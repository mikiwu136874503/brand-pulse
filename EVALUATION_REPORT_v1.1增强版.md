# AI Agent 编码能力评估报告（v1.1 增强版）

> **项目名称**：Brand Pulse — 竞争品牌内容雷达  
> **开发者**：Miki  
> **日期**：2026年5月30日  
> **评估对象**：基于 Cursor AI Agent 的全流程自然语言驱动开发  
> **报告版本**：v1.1 增强版  
> **项目仓库**：`brand-pulse`

---

## 1. 项目概述

### 1.1 工具简介

**Brand Pulse** 是一款面向品牌内容团队的 Web 分析工具，用于自动采集竞争品牌的 RSS / 网页内容，借助 DeepSeek API 完成分类、摘要、调性分析、双品牌对比、差距洞察，并一键生成公众号推文、社媒文案与内容日历等可执行内容策略包。

技术栈：Python、Streamlit、SQLite、Plotly、feedparser、requests、BeautifulSoup4、OpenAI SDK（DeepSeek 兼容接口）。

### 1.2 Miki · 角色说明

| 维度 | 说明 |
|------|------|
| **编程基础** | 零编程基础，未手写业务逻辑代码 |
| **开发方式** | 全程使用自然语言在 Cursor 中驱动 AI Agent 完成需求描述、迭代修复与验收 |
| **人工职责** | 提出需求、上传截图反馈 UI 问题、确认功能是否符合预期、配置 `.env` API Key |
| **AI 职责** | 架构设计、模块实现、CSS 样式、Bug 修复、README / 依赖管理、交付审核 |

### 1.3 交付成果概览

- **7 项核心功能**全部实现并可演示
- **全中文界面**，品牌与内容源可配置
- **`python app.py` 一键启动**，无需手动输入 `streamlit run`
- 模块化代码结构：`app.py` + 10 余个业务模块（`db.py`、`rss_fetcher.py`、`web_scraper.py` 等）

### 1.4 v1.1 增强说明

本版本在 **v1.0 交付版**基础上新增了以下增强功能：**收藏文章**、**自定义样本对比**、**时效性筛选**、**一键更新全部品牌**，由零编程基础开发者通过 **Cursor AI** 驱动迭代完成。

---

## 2. 功能模块实现记录

> 以下数据为开发全过程的**合理估算**，供评估报告使用；非精确计时，但反映真实迭代量级。

| 功能模块 | Prompt 轮次（估） | Agent 用时（估） | 人工介入次数（估） | 自修复情况（估） |
|----------|------------------|------------------|--------------------|------------------|
| 数据库 + RSS 采集 | 2–3 | 25 min | 1 | `save_articles()` 返回值解包错误（单 int → 元组） |
| 网页抓取 + source_type | 2 | 20 min | 1 | URL 自动识别 RSS / 网页逻辑 |
| AI 分类 / 摘要 | 2 | 15 min | 0 | — |
| 品牌调性分析 | 2–3 | 30 min | 1 | AI 返回 JSON 字段名兼容（中文字段 / undefined 过滤） |
| 品牌对比 + 词云 | 3–4 | 35 min | 1 | Plotly 废弃 API `titlefont` → `title_font` |
| 主题分布对比 | 2 | 20 min | 1 | 多品牌调性缓存复用、HTML 表格替代 st.dataframe |
| 差距分析 | 1–2 | 15 min | 0 | — |
| 内容策略生成 | 2–3 | 25 min | 2 | 品牌不一致警告 → 与 gap 分析结果强制绑定 |
| UI / CSS / 导航 | 6–10 | 90 min | 5+ | select 点击失效、undefined 文字、导航宽度与对比度 |
| 启动包装 + README + 依赖锁定 | 1–2 | 15 min | 0 | `if __name__ == "__main__"` + 版本约束 |
| 收藏文章功能 | 2 | 15 min | 1 | 数据库字段迁移兼容 |
| 自定义样本对比 | 4 | 35 min | 2 | 跨筛选勾选丢失修复、get_articles_by_ids 缺失 |
| 时效性筛选 | 2 | 20 min | 1 | 时间字段比较格式适配 |
| 一键更新全部品牌 | 1 | 10 min | 0 | 无 |
| UI 按钮对齐修复 | 2 | 15 min | 1 | 列布局 vertical_alignment |
| **合计** | **34–45 轮** | **约 6–7.5 h** | **17–20 次** | **13–15 处** |

### 2.1 功能与任务书对照（复审结论）

| # | 任务书要求 | 实现状态 |
|---|------------|----------|
| 1 | 品牌内容采集（RSS / 网页抓取） | ✅ 已实现 |
| 2 | AI 自动分类 | ✅ 已实现 |
| 3 | AI 摘要生成 | ✅ 已实现 |
| 4 | 品牌调性分析 | ✅ 已实现 |
| 5 | 品牌对比（含主题差异） | ✅ 已实现（主题对比需先执行调性分析） |
| 6 | 差距分析 | ✅ 已实现 |
| 7 | 内容生成 | ✅ 已实现 |

---

## 3. 典型案例分析

### 案例一：`TypeError` 返回值解包错误

**问题描述**  
在「品牌内容管理」页点击「立即采集」后，页面报错：`ValueError` / `TypeError: cannot unpack non-iterable int object`。原因是 `db.save_articles()` 在某一版本中只返回新增数量 `int`，而 `app.py` 中采集成功逻辑按 `(new_count, skipped_count)` 元组解包。

**AI 行为**  
Agent 通过报错栈定位到 `app.py` 的解包语句与 `db.py` 的返回值不一致，将 `save_articles()` 修改为返回 `(new_count, skipped_count)` 元组，并统一所有调用处的解包逻辑。修复后采集成功提示可正确显示「新增 X 篇，跳过 Y 篇重复文章」。

**我的角色**  
将浏览器中的完整报错信息粘贴给 AI，说明「点击立即采集后崩溃」，未阅读代码。

**启示**  
- 函数返回值契约变更时，AI 需全局搜索所有调用点，而非只改一处。  
- 零代码基础用户最有效的反馈方式是：**操作步骤 + 完整报错原文**。  
- 此类 Bug 属于 AI 可 100% 自主修复的类型，无需用户理解 Python 语法。

---

### 案例二：UI 按钮文字对比度与深色主题穿透

**问题描述**  
应用采用深色科技风主题（背景 `#0B0F19`、强调色 `#00D4FF`）后，出现多处 UI 可读性问题：侧边栏导航三项文字过暗看不清、选中高亮条宽度与侧边栏不一致、分类标签与按钮文字对比度不足、Plotly 图表在深色背景下样式不统一。

**AI 行为**  
Agent 在 `inject_global_css()` 中增加穿透式 CSS：统一按钮 `-webkit-text-fill-color`、侧边栏导航改为按钮式 + 全宽布局、分类标签改为实心背景、Plotly 通过 `style_plotly_fig()` 统一深色样式。多轮根据用户截图迭代颜色值（未选中 `#B0C4DE`、选中左侧青色竖线 + 浅蓝底）。

**我的角色**  
上传 UI 截图，用自然语言描述「字看不清」「高亮条太短」「要和 DeepSeek API 已配置那条一样宽」等视觉预期，充当**视觉验收官**。

**启示**  
- UI 类需求**截图驱动**效率远高于纯文字描述。  
- Streamlit 默认样式与自定义 CSS 冲突多，需要多轮人工视觉验收。  
- 人类在深色主题、间距、对齐方面的作用不可替代；AI 负责写 CSS，人类负责「好不好看」。

---

### 案例三：下拉框全部失效的修复

**问题描述**  
为消除 selectbox「双边框 / 文字后条状图形」等视觉问题，Agent 对 `[data-baseweb="select"] input` 添加了 `display: none` 与 `pointer-events: none`。结果**全站所有下拉框**（品牌筛选、对比选品牌等）均无法点击展开，功能完全失效。

**AI 行为**  
收到用户反馈「所有选择框点不了」后，Agent 排查 CSS 变更历史，定位到 hidden input 被禁用点击是 Streamlit / BaseWeb 组件的交互层。修复方案：  
- 仅对原生 `<select>` 做视觉隐藏（移出视口），**不**禁用 `div[data-baseweb="select"] input` 的 pointer-events；  
- 改为只去除 border / background，保留可点击性；  
- 侧边栏导航从 `st.radio` CSS 改造改为 `st.button` + session_state，减少与 Streamlit 内部 DOM 的对抗。

**我的角色**  
发现回归问题后立即反馈「所有页面的下拉框都失效了」，并补充早期截图对比，帮助 AI 关联到 CSS 修改批次。

**启示**  
- **CSS 全局覆盖是高风险操作**，一处 `pointer-events: none` 可导致整类组件瘫痪。  
- 回归测试不能只验视觉，必须验**交互**（能点、能选、能提交）。  
- AI 自修复能力强，但引入新 Bug 的速度也快；人类需做「功能冒烟」验收。

---

### 案例四：跨筛选勾选持久化修复

**问题描述**  
切换品牌筛选后，之前在列表中勾选的文章会丢失，无法用于自定义样本对比等依赖多选状态的功能。

**AI 行为**  
引入 `st.session_state.checked_article_ids` 全局集合维护勾选状态，使勾选与当前表格筛选条件解耦，跨品牌筛选切换后仍保留已选文章 ID。

**我的角色**  
测试跨品牌筛选场景并反馈（先选 A 品牌文章 → 切换 B 品牌筛选 → 确认勾选是否仍在）。

**启示**  
复杂交互需明确状态管理边界：哪些状态应绑定当前视图、哪些应全局持久，需在需求阶段与实现阶段对齐。

---

## 4. 总结与反思

v1.1 增强版的迭代证明，**AI-native 工作流**不仅能完成初始交付，更能快速响应场景化需求，实现产品持续进化。

### 4.1 AI 独立完成 vs 必须人类补充

| 类别 | AI 独立完成度高 | 需人类补充 |
|------|-----------------|------------|
| **后端逻辑** | 数据库设计、RSS/网页抓取、AI Prompt、JSON 解析、重试机制 | 提供 API Key、确认 RSS 源是否有效 |
| **前端功能** | 三页导航、表单、图表、批量操作、session 缓存 | 截图反馈 UI 问题、验收交互是否正常 |
| **样式** | 深色主题 CSS、Plotly 统一样式 | 视觉审美、间距宽度、「好不好看」 |
| **工程化** | 模块化拆分、README、requirements 版本锁定 | 决定是否提交 Git、部署环境选择 |
| **测试** | 根据报错自修复 | 端到端走查（采集→分类→对比→生成） |

### 4.2 Prompt 策略优化心得

1. **一条 Prompt 只做一类事**：功能开发、UI 修复、审核验收分开提，避免 AI 同时大改逻辑与样式。  
2. **截图 + 操作步骤 + 期望结果** 是零代码用户最有效的反馈格式。  
3. **引用任务书原文**作为验收标准，便于 Agent 对照而非自由发挥。  
4. **明确约束**：「不改业务逻辑，只改样式」「不要引入新依赖」可显著降低回归风险。  
5. **复审请求**：开发结束后单独要「严格审核官」式对照检查，能系统性发现 README 滞后、功能缺口等问题。

### 4.3 若再做一次，会如何改进

| 改进项 | 具体做法 |
|--------|----------|
| **早期约定模块边界** | 第一轮就要求 `app.py` 只做路由，业务放独立文件，减少后期 1700 行单文件 |
| **UI 改动前先列验收清单** | 每轮 CSS 修改后检查：下拉框、按钮、表格、图表、侧边栏共 5 项 |
| **减少 CSS 对抗 Streamlit** | 优先用 Streamlit 原生组件参数，CSS 仅做配色 |
| **功能开发优先于美化** | 7 项功能闭环后再统一做深色主题，避免 UI 迭代占用 6–10 轮 Prompt |
| **保留 Agent 对话记录** | 便于写评估报告、复盘 Prompt 轮次与自修复次数 |

---

## 5. 结论

本项目**完全满足任务书要求**，具体体现在：

1. **7 项核心功能全部实现**：RSS / 网页采集、AI 分类、AI 摘要、品牌调性分析、品牌对比（含主题分布对比）、差距分析、内容策略生成。  
2. **技术栈符合规范**：Python + Streamlit + DeepSeek API + feedparser / requests + SQLite + Plotly，并扩展 beautifulsoup4、jieba、wordcloud 等。  
3. **非功能要求达标**：全中文界面；品牌与 RSS / 网页地址可配置；API Key 从环境变量读取。  
4. **可用性达标**：`pip install -r requirements.txt` 后执行 `python app.py` 即可启动，README 与 `.env.example` 齐全。  
5. **代码质量可交付**：模块清晰、关键路径有异常处理与中文错误提示；经多轮 UI 与逻辑自修复后稳定可演示。

**综合评定**：在「零编程基础 + 自然语言驱动 Cursor AI Agent」模式下，本项目证明了 AI 可独立完成**约 85–90%** 的编码与修复工作；剩余 **10–15%** 主要为视觉验收、交互回归测试与 API / 数据源配置。作为 AI Agent 编码能力评估样本，**达到预期交付标准，可作为完整案例归档**。

---

*本报告由Miki的项目开发过程与两次交付审核记录整理生成，可直接纳入 GitHub 仓库作为 AI 辅助开发评估文档。*
